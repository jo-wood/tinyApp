
<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
      integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
    <title>TinyApp</title>
  </head>

  <header>
    <%- include("partials/_header"); -%>
  </header>


  <body style="font-size:20px; padding:50px;">

<% if (userName) { %>
    <h1>Here are your short urls:</h1>
    <button>
      <a href='/urls/new'>+ new tinyURL</a>
    </button>

    <% for (let url in urls) { %>
    <% let shortForm = url  %>
    <% let longForm = urls[shortForm] %>
    <ul>
      <!-- <li>Long: <%= longForm %></li> -->
      <li>Short:
        <a href="/urls/<%=shortForm %>" >
          <%= shortForm %>
        </a>
      <%= longForm %>
      </li>
    
      <a href='/urls/<%=shortForm %>/update'>Edit Source URL</a>

    <form method="POST" action="/urls/<%= shortForm %>/delete">
      <label for="<%= shortForm %>">
      <input name="<%= shortForm %>" type="submit" value="Delete">
    </label>
    </form>


    </ul>
    <% } %>


 <% } else { %>
  <h2>Shorten your long URLs to short & readable links. </h2>
<% } %>

  </body>

</html>