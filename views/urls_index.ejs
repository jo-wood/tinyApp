
<!DOCTYPE html>
<html lang="en">

 <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
      integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
    <title>TinyApp</title>
  </head>

  <header>
    <%- include("partials/_header"); -%>
  </header>


  <body style="font-size:20px; padding:50px;">

<% if (userName) { %>
    <h1>Here are your short urls:</h1>
    <form method="GET" action="/urls/new">
      <input  type="submit" value="create a tinyURL">
    </form>

    <% for (let url in urls) { %>
    <% let shortURL = url;  %>
    <% let longURL = urls[shortURL] %>
    <ul>
      <!-- <li>Long: <%= longURL %></li> -->
      <li>tiny URL: 
        <a href="/urls/<%=shortURL %>" > <%= shortURL %> </a> 
        <ul>
          <li> for URL: <%= longURL %> </li>
        </ul>
      </li>

    
      <a href='/urls/<%=shortURL %>'>Edit Source URL</a>

    <form method="POST" action="/urls/<%= shortURL %>/delete">
      <input name="<%= shortURL %>" type="submit" value="Delete">
  
    </form>


    </ul>
    <% } %>


<% } else { %>
  <h2>Shorten your long URLs to short & readable links. </h2>
<% } %>

  </body>

</html>